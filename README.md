
  # Portfolio Website with Chat

  This is a code bundle for Portfolio Website with Chat. The original project is available at https://www.figma.com/design/4yC4iTbCvCjNkBGBhrlyAL/Portfolio-Website-with-Chat.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  # Core
  - React (peerDependency: react 18.x)
  - React DOM (peerDependency: react-dom 18.x)
  - ECMAScript modules (type: "module")
  - TypeScript (inferred — project uses .tsx files)

  # Build / Dev
  - Vite
  - @vitejs/plugin-react
  - npm scripts: dev/build (vite)
  - pnpm (pnpm overrides present — likely used)

  # Styling / Theming
  - Tailwind CSS
  - @tailwindcss/vite
  - tw-animate-css
  - tailwind-merge
  - Emotion (@emotion/react, @emotion/styled)
  - MUI (@mui/material, @mui/icons-material)

  # UI primitives / component libraries
  - Radix UI (many @radix-ui/react-* packages: accordion, popover, select, tooltip, tabs, slider, switch, scroll-area, etc.)
  - Lucide React (icons)
  - cmdk (command palette)
  - sonner (toasts/notifications)
  - vaul

  # Layout / UX widgets
  - embla-carousel-react
  - react-slick
  - react-responsive-masonry
  - react-resizable-panels
  - react-day-picker

  # Forms / inputs / validation
  - react-hook-form
  - input-otp

  # Drag & drop / interactions
  - react-dnd
  - react-dnd-html5-backend

  # Positioning / poppers
  - @popperjs/core
  - react-popper

  # Charts / data viz
  - recharts
  - date-fns (date utilities)

  # Utility / helper libs
  - clsx
  - class-variance-authority

  # Animation
  - motion (motion/react)

